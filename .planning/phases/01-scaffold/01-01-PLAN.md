---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.node.json
  - vite.config.ts
  - index.html
  - .eslintrc.cjs
  - .prettierrc.json
  - .prettierignore
  - .gitignore
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/router.tsx
  - src/vite-env.d.ts
  - src/tokens/index.css
  - src/layouts/AuthLayout.tsx
  - src/layouts/AppLayout.tsx
  - src/components/NotFound.tsx
  - src/flows/splash/screens/SplashScreen.tsx
  - src/flows/welcome/screens/WelcomeScreen.tsx
  - src/flows/onboarding/screens/OnboardingScreen.tsx
  - src/flows/register/screens/RegisterScreen.tsx
  - src/flows/login/screens/LoginScreen.tsx
  - src/flows/home/screens/HomeScreen.tsx
  - src/flows/track/screens/TrackScreen.tsx
  - src/flows/wallet/screens/WalletScreen.tsx
  - src/flows/insights/screens/InsightsScreen.tsx
  - src/flows/profile/screens/ProfileScreen.tsx
autonomous: true
requirements: [FND-01, FND-02, FND-03, FND-04]

must_haves:
  truths:
    - "npm run dev starts without errors and serves the app at localhost"
    - "npm run build succeeds and emits separate JS chunks per lazy route in dist/assets/"
    - "npm run preview serves the built app successfully"
    - "npx tsc --noEmit exits 0 (TypeScript strict mode passes)"
    - "Visiting /, /welcome, /onboarding, /register, /login, /app, /app/track, /app/wallet, /app/insights, /app/profile each renders a placeholder without a blank screen"
    - "Visiting an unknown route renders the Not Found placeholder"
    - "index.html meta viewport includes viewport-fit=cover"
    - "Both AuthLayout and AppLayout render Outlet and use min-h-[100dvh]"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "react-router-dom"
    - path: "index.html"
      provides: "HTML entry with mobile viewport"
      contains: "viewport-fit=cover"
    - path: "vite.config.ts"
      provides: "Vite config with React + Tailwind v4 plugin + path alias"
      contains: "@tailwindcss/vite"
    - path: "tsconfig.json"
      provides: "TypeScript strict config with @/* alias"
      contains: "strict"
    - path: "src/index.css"
      provides: "Tailwind import + tokens import + mobile baseline"
      contains: "@import"
    - path: "src/tokens/index.css"
      provides: "Empty @theme file for Phase 2 to populate"
      contains: "@theme"
    - path: "src/router.tsx"
      provides: "createBrowserRouter with layout routes and lazy imports"
      contains: "createBrowserRouter"
    - path: "src/layouts/AuthLayout.tsx"
      provides: "Auth-side layout wrapper with Outlet"
      contains: "Outlet"
    - path: "src/layouts/AppLayout.tsx"
      provides: "App-side layout wrapper with Outlet"
      contains: "Outlet"
  key_links:
    - from: "src/main.tsx"
      to: "src/router.tsx"
      via: "RouterProvider import"
      pattern: "RouterProvider"
    - from: "src/router.tsx"
      to: "src/layouts/AuthLayout.tsx + AppLayout.tsx"
      via: "lazy imports + route element"
      pattern: "React.lazy"
    - from: "src/layouts/AuthLayout.tsx"
      to: "child routes"
      via: "Outlet element"
      pattern: "Outlet"
    - from: "src/layouts/AppLayout.tsx"
      to: "child routes"
      via: "Outlet element"
      pattern: "Outlet"
    - from: "index.html"
      to: "mobile viewport behavior"
      via: "viewport-fit=cover meta"
      pattern: "viewport-fit=cover"
---

<objective>
Scaffold a working Vite + React 18 + TypeScript 5 (strict) + Tailwind CSS v4 + React Router v6 mobile-web application with correct viewport configuration, layout routes with Outlet, and route-level code splitting via React.lazy + Suspense — establishing every foundational pattern before any token, primitive, or screen work begins.

Purpose: Phase 1 is infrastructure only. It permanently prevents the four foundational pitfalls (100vh, missing safe-area, missing Outlet, missing code splitting) so they cannot surface in later phases. No design tokens, no primitives, no real UI — just a correctly structured skeleton that boots, builds, and routes.

Output: A Vite project in the repository root with the complete src/ folder structure, all 10 placeholder flow screens, two layout components, router config, ESLint + Prettier config, and passing npm run dev, npm run build, npm run preview, and npx tsc --noEmit.
</objective>

<execution_context>
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scaffold/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md

<interfaces>
<!-- This plan creates all interfaces from scratch (greenfield).
     The contracts below are what the plan PRODUCES.
     Executor should implement exactly these shapes. -->

Placeholder screen component signature (all 10 screens + NotFound share this shape):
- Default export required for React.lazy() to work
- Signature: export default function SplashScreen(): JSX.Element

Layout component signature:
- Default export required for React.lazy()
- MUST render Outlet from react-router-dom
- MUST apply min-h-[100dvh] and max-w-md mx-auto for mobile shell
- Signatures: export default function AuthLayout() and export default function AppLayout()

Router shape (src/router.tsx):
- Exports const router = createBrowserRouter([...])
- Two top-level layout routes (/ -> AuthLayout, /app -> AppLayout)
- Catch-all * -> NotFound
- Every layout and screen wrapped in Suspense fallback around a React.lazy import
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite + React + TS project and install core dependencies</name>
  <files>package.json, package-lock.json, tsconfig.json, tsconfig.node.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/vite-env.d.ts, .gitignore</files>
  <read_first>
    - .planning/phases/01-scaffold/01-CONTEXT.md (decisions for stack, versions, package manager)
    - .planning/research/STACK.md (library versions, Vite/React/TS baseline)
  </read_first>
  <action>
    Initialize a Vite React-TS project IN-PLACE in the current working directory (do NOT create a subdirectory — files go at repo root).

    Step 1: Create package.json at repo root with exact content:
    ```
    {
      "name": "costra-frontend",
      "private": true,
      "version": "0.0.1",
      "type": "module",
      "scripts": {
        "dev": "vite",
        "build": "tsc -b && vite build",
        "preview": "vite preview",
        "lint": "eslint src --ext .ts,.tsx",
        "format": "prettier --write \"src/**/*.{ts,tsx,css,json}\""
      }
    }
    ```

    Step 2: Install dependencies sequentially:
    - npm install react@^18.3.1 react-dom@^18.3.1 react-router-dom@^6.26.0
    - npm install -D typescript@^5.5.0 @types/react@^18.3.0 @types/react-dom@^18.3.0 @types/node@^20.0.0 vite@^5.4.0 @vitejs/plugin-react@^4.3.0 tailwindcss@^4.0.0 @tailwindcss/vite@^4.0.0

    Versions are minimum floors. If npm resolves a higher compatible version, accept it. If tailwindcss v4 or @tailwindcss/vite fails to resolve, fall back to the latest available and note it in the SUMMARY.

    Step 3: Create tsconfig.json (strict mode + path alias):
    ```
    {
      "compilerOptions": {
        "target": "ES2022",
        "useDefineForClassFields": true,
        "lib": ["ES2022", "DOM", "DOM.Iterable"],
        "module": "ESNext",
        "skipLibCheck": true,
        "moduleResolution": "bundler",
        "allowImportingTsExtensions": true,
        "resolveJsonModule": true,
        "isolatedModules": true,
        "noEmit": true,
        "jsx": "react-jsx",
        "strict": true,
        "noUnusedLocals": true,
        "noUnusedParameters": true,
        "noFallthroughCasesInSwitch": true,
        "noUncheckedIndexedAccess": true,
        "exactOptionalPropertyTypes": true,
        "baseUrl": ".",
        "paths": { "@/*": ["src/*"] }
      },
      "include": ["src"],
      "references": [{ "path": "./tsconfig.node.json" }]
    }
    ```

    Step 4: Create tsconfig.node.json:
    ```
    {
      "compilerOptions": {
        "composite": true,
        "skipLibCheck": true,
        "module": "ESNext",
        "moduleResolution": "bundler",
        "allowSyntheticDefaultImports": true,
        "strict": true
      },
      "include": ["vite.config.ts"]
    }
    ```

    Step 5: Create vite.config.ts:
    ```
    import { defineConfig } from 'vite';
    import react from '@vitejs/plugin-react';
    import tailwindcss from '@tailwindcss/vite';
    import path from 'node:path';

    export default defineConfig({
      plugins: [react(), tailwindcss()],
      resolve: {
        alias: { '@': path.resolve(__dirname, './src') },
      },
    });
    ```

    Step 6: Create index.html at repo root (MUST include viewport-fit=cover):
    ```
    <!doctype html>
    <html lang="en">
      <head>
        <meta charset="UTF-8" />
        <link rel="icon" type="image/svg+xml" href="/vite.svg" />
        <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
        <meta name="theme-color" content="#ffffff" />
        <title>Costra</title>
      </head>
      <body class="touch-manipulation">
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
    </html>
    ```

    Step 7: Create src/vite-env.d.ts:
    ```
    /// <reference types="vite/client" />
    ```

    Step 8: Create minimal src/main.tsx and src/App.tsx stubs (App.tsx is replaced in Task 5):
    ```
    // src/main.tsx
    import React from 'react';
    import ReactDOM from 'react-dom/client';
    import App from './App';
    import './index.css';

    ReactDOM.createRoot(document.getElementById('root')!).render(
      <React.StrictMode>
        <App />
      </React.StrictMode>,
    );
    ```
    ```
    // src/App.tsx — temporary stub, replaced in Task 5
    export default function App() {
      return <div>Loading…</div>;
    }
    ```

    Step 9: Create .gitignore:
    ```
    node_modules
    dist
    dist-ssr
    *.local
    .vscode/*
    !.vscode/extensions.json
    .DS_Store
    *.log
    ```

    Step 10: Run npm install once more to ensure the lockfile is in sync.

    Do NOT install: zustand, react-hook-form, zod, recharts, lucide-react, framer-motion, date-fns, @fontsource/nunito-sans, tailwindcss-safe-area. Those belong to Phase 2+.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['react-router-dom']) process.exit(1); if(!p.devDependencies['tailwindcss']) process.exit(2); if(!p.devDependencies['@tailwindcss/vite']) process.exit(3); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "react-router-dom" in dependencies
    - package.json contains "tailwindcss" and "@tailwindcss/vite" in devDependencies
    - package.json contains "typescript" and "vite" in devDependencies
    - tsconfig.json contains "strict": true
    - tsconfig.json contains "@/*" path alias mapping to "src/*"
    - tsconfig.json contains "noUncheckedIndexedAccess": true
    - vite.config.ts contains "@tailwindcss/vite"
    - vite.config.ts contains "@" alias pointing to "./src"
    - index.html contains the string "viewport-fit=cover"
    - index.html contains "touch-manipulation" on body
    - node_modules/ directory exists
    - package-lock.json exists
  </acceptance_criteria>
  <done>Vite project scaffold exists at repo root with all dependencies installed, tsconfig strict mode enabled, path alias configured, and viewport meta correct.</done>
</task>

<task type="auto">
  <name>Task 2: Create CSS entry, tokens stub, and folder structure</name>
  <files>src/index.css, src/tokens/index.css, src/hooks/.gitkeep, src/utils/.gitkeep, src/types/.gitkeep, src/data/.gitkeep, src/components/primitives/.gitkeep</files>
  <read_first>
    - .planning/phases/01-scaffold/01-CONTEXT.md (folder structure section, CSS decisions)
    - .planning/research/ARCHITECTURE.md (folder layout)
    - .planning/research/PITFALLS.md (pitfall #2 viewport, #4 100vh, #5 safe-area)
  </read_first>
  <action>
    Step 1: Create src/tokens/index.css — an empty @theme block that Phase 2 will populate. The file MUST exist so the import path in index.css is valid:
    ```
    /* src/tokens/index.css
     * Design tokens for Costra — populated in Phase 2 (Design Tokens).
     * Phase 1 creates this empty so the import path is valid.
     */
    @theme {
      /* Populated in Phase 2 */
    }
    ```

    Step 2: Create src/index.css — Tailwind v4 import + tokens import + mobile-web baseline:
    ```
    @import "tailwindcss";
    @import "./tokens/index.css";

    /* Mobile-web baseline — prevents iOS 300ms tap delay, pull-to-refresh, rubberband. */
    html,
    body {
      height: 100%;
      margin: 0;
      padding: 0;
      overscroll-behavior: none;
      -webkit-tap-highlight-color: transparent;
    }

    body {
      touch-action: manipulation;
      font-family: system-ui, -apple-system, "Segoe UI", sans-serif;
      /* Nunito Sans wiring deferred to Phase 2 */
    }

    #root {
      min-height: 100dvh;
    }
    ```

    Step 3: Create the empty folder structure by placing empty .gitkeep files so git tracks the empty directories:
    - src/hooks/.gitkeep
    - src/utils/.gitkeep
    - src/types/.gitkeep
    - src/data/.gitkeep
    - src/components/primitives/.gitkeep

    Note: src/flows/*/screens/ directories are created implicitly in Task 4. src/layouts/ is created in Task 3.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/index.css','src/tokens/index.css','src/hooks/.gitkeep','src/utils/.gitkeep','src/types/.gitkeep','src/data/.gitkeep','src/components/primitives/.gitkeep']; for(const f of files){ if(!fs.existsSync(f)){ console.error('missing',f); process.exit(1);} } const css=fs.readFileSync('src/index.css','utf8'); if(!css.includes('@import')) process.exit(2); if(!css.includes('tailwindcss')) process.exit(3); if(!css.includes('100dvh')) process.exit(4); if(!css.includes('touch-action: manipulation')) process.exit(5); const tok=fs.readFileSync('src/tokens/index.css','utf8'); if(!tok.includes('@theme')) process.exit(6); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains @import "tailwindcss"
    - src/index.css contains @import "./tokens/index.css"
    - src/index.css contains 100dvh
    - src/index.css contains touch-action: manipulation
    - src/index.css contains overscroll-behavior: none
    - src/tokens/index.css contains @theme
    - src/hooks/, src/utils/, src/types/, src/data/, src/components/primitives/ directories exist (via .gitkeep)
  </acceptance_criteria>
  <done>CSS entry imports Tailwind and tokens with mobile baseline rules; tokens stub exists with empty @theme; placeholder folders created.</done>
</task>

<task type="auto">
  <name>Task 3: Create AuthLayout and AppLayout with Outlet and mobile shell</name>
  <files>src/layouts/AuthLayout.tsx, src/layouts/AppLayout.tsx</files>
  <read_first>
    - .planning/phases/01-scaffold/01-CONTEXT.md (layout decisions, placeholder UX)
    - .planning/research/PITFALLS.md (pitfall #5 missing Outlet, #4 100vh, safe-area)
    - .planning/research/ARCHITECTURE.md (Pattern 2: Layout routes)
  </read_first>
  <action>
    Both layouts MUST: (a) render Outlet from react-router-dom; (b) use min-h-[100dvh] (NOT min-h-screen which maps to 100vh); (c) constrain to phone width via max-w-md mx-auto; (d) use export default (required for React.lazy in Task 5).

    Step 1: Create src/layouts/AuthLayout.tsx:
    ```
    import { Outlet } from 'react-router-dom';

    export default function AuthLayout() {
      return (
        <div className="mx-auto flex min-h-[100dvh] max-w-md flex-col bg-white">
          <header className="px-6 pt-[calc(env(safe-area-inset-top)+1rem)] pb-4 text-center">
            <h1 className="text-lg font-semibold tracking-tight">Costra</h1>
          </header>
          <main className="flex flex-1 flex-col">
            <Outlet />
          </main>
        </div>
      );
    }
    ```

    Step 2: Create src/layouts/AppLayout.tsx:
    ```
    import { Outlet } from 'react-router-dom';

    export default function AppLayout() {
      return (
        <div className="mx-auto flex min-h-[100dvh] max-w-md flex-col bg-gray-50">
          <header className="px-6 pt-[calc(env(safe-area-inset-top)+1rem)] pb-4">
            <h1 className="text-lg font-semibold tracking-tight">Costra App</h1>
          </header>
          <main className="flex-1 overflow-y-auto px-6 pb-[calc(env(safe-area-inset-bottom)+4rem)]">
            <Outlet />
          </main>
          {/* BottomNav slot — reserved height for Phase 3 primitive */}
          <div
            aria-hidden="true"
            className="pointer-events-none fixed inset-x-0 bottom-0 mx-auto h-16 max-w-md border-t border-gray-200 bg-white/80"
          >
            <div className="flex h-full items-center justify-center text-xs text-gray-400">
              [BottomNav — Phase 3]
            </div>
          </div>
        </div>
      );
    }
    ```

    Critical: Both files MUST use export default (not named exports) because React.lazy() in Task 5 uses the default export convention.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const f of ['src/layouts/AuthLayout.tsx','src/layouts/AppLayout.tsx']){ const c=fs.readFileSync(f,'utf8'); if(!c.includes('<Outlet')) {console.error(f,'missing Outlet');process.exit(1);} if(!c.includes('min-h-[100dvh]')) {console.error(f,'missing dvh');process.exit(2);} if(!c.includes('export default')) {console.error(f,'missing default export');process.exit(3);} if(!c.includes('max-w-md')) {console.error(f,'missing max-w-md');process.exit(4);} } console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/AuthLayout.tsx contains <Outlet
    - src/layouts/AuthLayout.tsx contains min-h-[100dvh]
    - src/layouts/AuthLayout.tsx contains export default
    - src/layouts/AuthLayout.tsx contains max-w-md
    - src/layouts/AppLayout.tsx contains <Outlet
    - src/layouts/AppLayout.tsx contains min-h-[100dvh]
    - src/layouts/AppLayout.tsx contains export default
    - src/layouts/AppLayout.tsx contains max-w-md
    - src/layouts/AppLayout.tsx contains env(safe-area-inset-bottom)
  </acceptance_criteria>
  <done>Both layout components render Outlet, use 100dvh shell, max-w-md container, include safe-area insets, and export default.</done>
</task>

<task type="auto">
  <name>Task 4: Create 10 placeholder flow screens + NotFound component</name>
  <files>src/flows/splash/screens/SplashScreen.tsx, src/flows/welcome/screens/WelcomeScreen.tsx, src/flows/onboarding/screens/OnboardingScreen.tsx, src/flows/register/screens/RegisterScreen.tsx, src/flows/login/screens/LoginScreen.tsx, src/flows/home/screens/HomeScreen.tsx, src/flows/track/screens/TrackScreen.tsx, src/flows/wallet/screens/WalletScreen.tsx, src/flows/insights/screens/InsightsScreen.tsx, src/flows/profile/screens/ProfileScreen.tsx, src/components/NotFound.tsx</files>
  <read_first>
    - .planning/phases/01-scaffold/01-CONTEXT.md (placeholder UX section)
    - .planning/research/ARCHITECTURE.md (flow folder structure)
  </read_first>
  <action>
    Create 11 placeholder components. Each MUST: (a) use export default (required for React.lazy); (b) render its route name in a heading; (c) include navigation links; (d) be minimal — no tokens, no primitives, no styling beyond bare Tailwind utility classes.

    Template for each flow screen (substitute name and links):
    ```
    import { Link } from 'react-router-dom';

    export default function SplashScreen() {
      return (
        <section className="flex flex-1 flex-col items-center justify-center gap-4 p-6">
          <h2 className="text-2xl font-bold">Splash</h2>
          <p className="text-sm text-gray-500">Phase 1 placeholder</p>
          <Link to="/welcome" className="text-blue-600 underline">
            → Welcome
          </Link>
        </section>
      );
    }
    ```

    Create all 10 screens using the same structure, substituting the heading text and link targets:

    1. src/flows/splash/screens/SplashScreen.tsx — heading "Splash", link to /welcome
    2. src/flows/welcome/screens/WelcomeScreen.tsx — heading "Welcome", links to /onboarding and /login
    3. src/flows/onboarding/screens/OnboardingScreen.tsx — heading "Onboarding", link to /register
    4. src/flows/register/screens/RegisterScreen.tsx — heading "Register", link to /app
    5. src/flows/login/screens/LoginScreen.tsx — heading "Login", link to /app
    6. src/flows/home/screens/HomeScreen.tsx — heading "Home", links to /app/track, /app/wallet, /app/insights, /app/profile
    7. src/flows/track/screens/TrackScreen.tsx — heading "Track", link back to /app
    8. src/flows/wallet/screens/WalletScreen.tsx — heading "Wallet", link back to /app
    9. src/flows/insights/screens/InsightsScreen.tsx — heading "Insights", link back to /app
    10. src/flows/profile/screens/ProfileScreen.tsx — heading "Profile", link to / (logout simulation)

    Also create src/components/NotFound.tsx (own full-page layout since it may render without a layout wrapper):
    ```
    import { Link } from 'react-router-dom';

    export default function NotFound() {
      return (
        <div className="mx-auto flex min-h-[100dvh] max-w-md flex-col items-center justify-center gap-4 p-6">
          <h2 className="text-2xl font-bold">Not Found</h2>
          <p className="text-sm text-gray-500">The page you're looking for doesn't exist.</p>
          <Link to="/" className="text-blue-600 underline">
            ← Back to home
          </Link>
        </div>
      );
    }
    ```

    All 11 components MUST use export default.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['src/flows/splash/screens/SplashScreen.tsx','src/flows/welcome/screens/WelcomeScreen.tsx','src/flows/onboarding/screens/OnboardingScreen.tsx','src/flows/register/screens/RegisterScreen.tsx','src/flows/login/screens/LoginScreen.tsx','src/flows/home/screens/HomeScreen.tsx','src/flows/track/screens/TrackScreen.tsx','src/flows/wallet/screens/WalletScreen.tsx','src/flows/insights/screens/InsightsScreen.tsx','src/flows/profile/screens/ProfileScreen.tsx','src/components/NotFound.tsx']; for(const f of files){ if(!fs.existsSync(f)){console.error('missing',f);process.exit(1);} const c=fs.readFileSync(f,'utf8'); if(!c.includes('export default')){console.error(f,'no default export');process.exit(2);} } console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 flow screen files exist at the paths listed above
    - src/components/NotFound.tsx exists
    - Every screen file contains export default
    - Every screen file imports from react-router-dom
    - Every screen file renders a heading matching its route name
  </acceptance_criteria>
  <done>All 11 placeholder components exist with default exports; folder structure for all 9 flows established under src/flows/.</done>
</task>

<task type="auto">
  <name>Task 5: Wire router.tsx with lazy routes and update App.tsx</name>
  <files>src/router.tsx, src/App.tsx</files>
  <read_first>
    - .planning/phases/01-scaffold/01-CONTEXT.md (routing decisions)
    - .planning/research/ARCHITECTURE.md (Pattern 2: Layout routes, route tree example)
    - .planning/research/PITFALLS.md (pitfall #7 code splitting, #5 missing Outlet)
    - src/layouts/AuthLayout.tsx (confirm default export)
    - src/layouts/AppLayout.tsx (confirm default export)
  </read_first>
  <action>
    Step 1: Create src/router.tsx — every layout and every screen loaded via React.lazy so Vite emits separate chunks per route (FND-03 requirement):
    ```
    import { lazy, Suspense, type ReactNode } from 'react';
    import { createBrowserRouter } from 'react-router-dom';

    const AuthLayout = lazy(() => import('@/layouts/AuthLayout'));
    const AppLayout = lazy(() => import('@/layouts/AppLayout'));

    const SplashScreen = lazy(() => import('@/flows/splash/screens/SplashScreen'));
    const WelcomeScreen = lazy(() => import('@/flows/welcome/screens/WelcomeScreen'));
    const OnboardingScreen = lazy(() => import('@/flows/onboarding/screens/OnboardingScreen'));
    const RegisterScreen = lazy(() => import('@/flows/register/screens/RegisterScreen'));
    const LoginScreen = lazy(() => import('@/flows/login/screens/LoginScreen'));

    const HomeScreen = lazy(() => import('@/flows/home/screens/HomeScreen'));
    const TrackScreen = lazy(() => import('@/flows/track/screens/TrackScreen'));
    const WalletScreen = lazy(() => import('@/flows/wallet/screens/WalletScreen'));
    const InsightsScreen = lazy(() => import('@/flows/insights/screens/InsightsScreen'));
    const ProfileScreen = lazy(() => import('@/flows/profile/screens/ProfileScreen'));

    const NotFound = lazy(() => import('@/components/NotFound'));

    function withSuspense(node: ReactNode) {
      return (
        <Suspense
          fallback={
            <div className="mx-auto flex min-h-[100dvh] max-w-md items-center justify-center">
              <span className="text-sm text-gray-400">Loading…</span>
            </div>
          }
        >
          {node}
        </Suspense>
      );
    }

    export const router = createBrowserRouter([
      {
        path: '/',
        element: withSuspense(<AuthLayout />),
        children: [
          { index: true, element: withSuspense(<SplashScreen />) },
          { path: 'welcome', element: withSuspense(<WelcomeScreen />) },
          { path: 'onboarding', element: withSuspense(<OnboardingScreen />) },
          { path: 'register', element: withSuspense(<RegisterScreen />) },
          { path: 'login', element: withSuspense(<LoginScreen />) },
        ],
      },
      {
        path: '/app',
        element: withSuspense(<AppLayout />),
        children: [
          { index: true, element: withSuspense(<HomeScreen />) },
          { path: 'track', element: withSuspense(<TrackScreen />) },
          { path: 'wallet', element: withSuspense(<WalletScreen />) },
          { path: 'insights', element: withSuspense(<InsightsScreen />) },
          { path: 'profile', element: withSuspense(<ProfileScreen />) },
        ],
      },
      { path: '*', element: withSuspense(<NotFound />) },
    ]);
    ```

    Step 2: Replace src/App.tsx with RouterProvider wrapper:
    ```
    import { RouterProvider } from 'react-router-dom';
    import { router } from './router';

    export default function App() {
      return <RouterProvider router={router} />;
    }
    ```

    src/main.tsx already imports App and index.css from Task 1 — no changes needed.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('src/router.tsx','utf8'); if(!r.includes('createBrowserRouter')) process.exit(1); const m=r.match(/lazy\(/g); if(!m||m.length<12){console.error('lazy count',m&&m.length);process.exit(2);} if(!r.includes('Suspense')) process.exit(3); const a=fs.readFileSync('src/App.tsx','utf8'); if(!a.includes('RouterProvider')) process.exit(4); if(!a.includes('router')) process.exit(5); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - src/router.tsx contains createBrowserRouter
    - src/router.tsx contains at least 12 calls to lazy( (2 layouts + 10 flow screens + NotFound = 13)
    - src/router.tsx contains Suspense
    - src/router.tsx defines routes for /, /welcome, /onboarding, /register, /login, /app, /app/track, /app/wallet, /app/insights, /app/profile, and *
    - src/App.tsx contains RouterProvider
    - src/App.tsx imports router from ./router
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Router wires all layouts and screens via React.lazy + Suspense; App.tsx uses RouterProvider; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 6: Configure ESLint + Prettier + prettier-plugin-tailwindcss</name>
  <files>.eslintrc.cjs, .prettierrc.json, .prettierignore, package.json</files>
  <read_first>
    - .planning/phases/01-scaffold/01-CONTEXT.md (lint/format decisions)
    - package.json (current scripts/deps)
  </read_first>
  <action>
    Step 1: Install dev dependencies:
    npm install -D eslint@^8.57.0 @typescript-eslint/parser@^7.0.0 @typescript-eslint/eslint-plugin@^7.0.0 eslint-plugin-react@^7.34.0 eslint-plugin-react-hooks@^4.6.0 eslint-plugin-react-refresh@^0.4.0 eslint-plugin-jsx-a11y@^6.8.0 prettier@^3.2.0 prettier-plugin-tailwindcss@^0.5.0

    Note: ESLint 8 (not 9) to avoid the flat-config migration burden in Phase 1.

    Step 2: Create .eslintrc.cjs:
    ```
    /* eslint-env node */
    module.exports = {
      root: true,
      env: { browser: true, es2022: true, node: true },
      parser: '@typescript-eslint/parser',
      parserOptions: {
        ecmaVersion: 2022,
        sourceType: 'module',
        ecmaFeatures: { jsx: true },
      },
      settings: { react: { version: 'detect' } },
      extends: [
        'eslint:recommended',
        'plugin:@typescript-eslint/recommended',
        'plugin:react/recommended',
        'plugin:react/jsx-runtime',
        'plugin:react-hooks/recommended',
        'plugin:jsx-a11y/recommended',
      ],
      plugins: ['react-refresh'],
      rules: {
        'react-refresh/only-export-components': ['warn', { allowConstantExport: true }],
        '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
      },
      ignorePatterns: ['dist', 'node_modules', '.eslintrc.cjs', 'vite.config.ts'],
    };
    ```

    Step 3: Create .prettierrc.json:
    ```
    {
      "semi": true,
      "singleQuote": true,
      "trailingComma": "all",
      "printWidth": 100,
      "tabWidth": 2,
      "plugins": ["prettier-plugin-tailwindcss"]
    }
    ```

    Step 4: Create .prettierignore:
    ```
    node_modules
    dist
    package-lock.json
    .planning
    ```

    Step 5: Verify npm run lint and npm run format scripts exist in package.json (added in Task 1). If missing, add them.

    Step 6: Run npx prettier --write "src/**/*.{ts,tsx,css}" to apply consistent formatting to everything created in Tasks 1-5.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const f of ['.eslintrc.cjs','.prettierrc.json','.prettierignore']){ if(!fs.existsSync(f)){console.error('missing',f);process.exit(1);} } const p=require('./package.json'); if(!p.devDependencies['eslint']) process.exit(2); if(!p.devDependencies['prettier-plugin-tailwindcss']) process.exit(3); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - .eslintrc.cjs exists
    - .prettierrc.json exists and contains prettier-plugin-tailwindcss
    - .prettierignore exists
    - package.json devDependencies contains eslint, prettier, prettier-plugin-tailwindcss, @typescript-eslint/parser, @typescript-eslint/eslint-plugin
    - npx eslint src --ext .ts,.tsx exits 0 (no errors; warnings allowed)
  </acceptance_criteria>
  <done>ESLint and Prettier configured with Tailwind class sorting; all source files formatted; lint runs clean.</done>
</task>

<task type="auto">
  <name>Task 7: Verify build, typecheck, lint, and chunk splitting end-to-end</name>
  <files>(no file changes — verification only)</files>
  <read_first>
    - .planning/phases/01-scaffold/01-CONTEXT.md (verification success criteria)
    - .planning/ROADMAP.md (Phase 1 success criteria)
    - .planning/research/PITFALLS.md (pitfall #7 code splitting verification)
  </read_first>
  <action>
    Run the full verification suite. Each command must succeed. If any fails, diagnose and fix before proceeding to SUMMARY.

    Step 1: Typecheck
    - Run: npx tsc --noEmit
    - Expected: exit 0, no output.

    Step 2: Lint
    - Run: npx eslint src --ext .ts,.tsx
    - Expected: exit 0.

    Step 3: Production build
    - Run: npm run build
    - Expected: exit 0; dist/ directory created; dist/index.html present; dist/assets/ contains multiple .js files (NOT a single bundle).

    Step 4: Verify code-splitting produced separate chunks. Count the JS files in dist/assets/ — MUST be >= 10 (allowing some small-chunk merging). Run:
    node -e "const fs=require('fs'); const files=fs.readdirSync('dist/assets').filter(f=>f.endsWith('.js')); console.log('JS chunks:',files.length,files); if(files.length<10){console.error('Expected >=10 chunks, got',files.length);process.exit(1);}"

    Step 5: Preview the build in background, curl it, then kill:
    - Run: (npm run preview &) && sleep 3 && curl -sf http://localhost:4173/ | head -20
    - Expected: HTML containing <div id="root">. Kill the preview process afterward.

    Step 6: Start dev server briefly to confirm it boots without errors:
    - Run: (npm run dev &) && sleep 4 && curl -sf http://localhost:5173/ | head -5
    - Expected: HTML response. Kill the dev process afterward.

    Step 7: Final grep sanity checks on the key pitfalls:
    - grep -r "min-h-screen" src/ MUST return nothing (exit 1 from grep)
    - grep -r "100vh" src/ MUST return nothing
    - grep "viewport-fit=cover" index.html MUST match
    - grep "<Outlet" src/layouts/AuthLayout.tsx MUST match
    - grep "<Outlet" src/layouts/AppLayout.tsx MUST match

    If ALL steps pass, Phase 1 scaffold is complete. Proceed to SUMMARY creation.

    If any step fails: do NOT proceed. Diagnose the failure, fix the underlying issue (not the verification), and re-run the full suite.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src --ext .ts,.tsx && npm run build && node -e "const fs=require('fs'); const files=fs.readdirSync('dist/assets').filter(f=>f.endsWith('.js')); if(files.length<10){console.error('chunks',files.length);process.exit(1);} console.log('chunks:',files.length);"</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0
    - npx eslint src --ext .ts,.tsx exits 0
    - npm run build exits 0
    - dist/index.html exists after build
    - dist/assets/ contains at least 10 JS files (confirming code splitting)
    - npm run preview serves HTML containing <div id="root"> at localhost:4173
    - npm run dev serves HTML at localhost:5173
    - grep -r "min-h-screen" src/ returns no matches
    - grep -r "100vh" src/ returns no matches
    - grep "viewport-fit=cover" index.html matches
    - grep "<Outlet" src/layouts/AuthLayout.tsx matches
    - grep "<Outlet" src/layouts/AppLayout.tsx matches
  </acceptance_criteria>
  <done>All verification commands pass; Phase 1 success criteria from ROADMAP met; dev server boots, production build succeeds, code splitting confirmed in dist/assets/, all anti-pitfall greps clean.</done>
</task>

</tasks>

<verification>
Phase 1 is complete when all of the following are true:

1. Dev server boots clean: npm run dev starts without errors at localhost:5173
2. Production build succeeds: npm run build completes and emits >= 10 JS chunks in dist/assets/ (route-level code splitting confirmed)
3. Preview works: npm run preview serves the built app at localhost:4173
4. TypeScript strict passes: npx tsc --noEmit exits 0
5. Lint passes: npx eslint src --ext .ts,.tsx exits 0
6. Viewport correct: index.html contains viewport-fit=cover
7. dvh not vh: No occurrence of 100vh or min-h-screen anywhere in src/
8. Outlets wired: Both AuthLayout.tsx and AppLayout.tsx contain <Outlet
9. All routes resolve: Visit /, /welcome, /onboarding, /register, /login, /app, /app/track, /app/wallet, /app/insights, /app/profile, /nonexistent — each returns rendered HTML (no blank screens)
10. Safe-area present: AppLayout contains env(safe-area-inset-bottom)
</verification>

<success_criteria>
- FND-01: Project scaffolded with Vite + React + TypeScript (strict) + Tailwind CSS v4 + React Router v6 — verified by package.json deps and tsconfig strict flag
- FND-02: Mobile-web viewport configured (viewport-fit=cover in index.html, min-h-[100dvh] in layouts, env(safe-area-inset-*) in AppLayout) — verified by grep on index.html and layout files
- FND-03: Route-level code splitting via React.lazy + Suspense — verified by 10+ JS chunks in dist/assets/ after npm run build
- FND-04: AuthLayout and AppLayout layout routes with Outlet wired and smoke-tested — verified by grep and by visiting each route in the dev server

All four phase requirements addressed in this single plan. No checkpoint tasks; plan is fully autonomous.
</success_criteria>

<output>
After all 7 tasks pass their acceptance criteria, create .planning/phases/01-scaffold/01-01-SUMMARY.md documenting:
- Actual versions installed for each package (resolved from package-lock.json)
- Any deviations from the planned versions (e.g., Tailwind v4 fallback if not yet stable)
- File tree snapshot of src/
- Output of npm run build (chunk count and sizes)
- Confirmation of all 4 requirement IDs (FND-01..04) satisfied
- Patterns established for Phase 2+ to inherit (folder structure, lazy import convention, layout+Outlet pattern, dvh shell)
</output>
