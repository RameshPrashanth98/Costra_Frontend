---
id: 260406-gi5
type: quick
title: Build Login Screen HIFI with OTP sign-in
files_modified:
  - src/flows/login/screens/LoginScreen.tsx
  - src/flows/onboarding/screens/OnboardingScreen.tsx
autonomous: true
---

<objective>
Replace the LoginScreen placeholder with a high-fidelity OTP sign-in screen matching the Costra design system, and update OnboardingScreen navigation to route to /login instead of /register.

Purpose: Deliver a polished, production-quality login screen consistent with the Splash and Onboarding screens already built.
Output: Fully styled LoginScreen with OTP mobile input, navigation wiring from onboarding.
</objective>

<context>
@.planning/STATE.md
@src/flows/onboarding/screens/OnboardingScreen.tsx (pattern reference for StatusBar, CostraBrand, inline styles, CSS keyframe animations, fadeUp helper)
@src/flows/login/screens/LoginScreen.tsx (current placeholder to replace)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build HIFI LoginScreen with OTP sign-in UI</name>
  <files>src/flows/login/screens/LoginScreen.tsx</files>
  <action>
Replace the entire LoginScreen placeholder with a HIFI implementation. Follow the exact same patterns as OnboardingScreen: inline styles, CSS keyframe animations via injected style tag, StatusBar and CostraBrand components defined locally (copy from OnboardingScreen — no shared component extraction yet).

Structure (top to bottom, full-height dark layout):
1. Inject CSS keyframes for fadeUp animation (same as OnboardingScreen)
2. StatusBar component — identical to OnboardingScreen's StatusBar
3. CostraBrand component — identical to OnboardingScreen's CostraBrand, placed ~60px from top
4. Eyebrow label: "OTP SIGN-IN" — JetBrains Mono, 0.6rem, uppercase, letterSpacing 0.2em, color #C8FF00, fontWeight 500
5. Heading: "Login with your mobile number." — Outfit, fontWeight 800, fontSize 1.65rem, lineHeight 1.2, color #FAFAFA
6. Body text: "Use the phone number you trust for daily alerts, secure backup, and fast access across devices." — Outfit, fontWeight 300, fontSize 0.85rem, lineHeight 1.65, color #A1A1AA
7. Card (background #0A0A0B, borderRadius 16, border 1px solid #1C1C1F, padding 20px):
   - Label: "MOBILE NUMBER" — JetBrains Mono, 0.6rem, uppercase, letterSpacing 0.18em, color #52525B, fontWeight 500, marginBottom 12
   - Input row (flex, gap 10, alignItems center):
     - Country code chip: "+94" in a div with background #141416, border 1px solid #2A2A2E, borderRadius 10, padding "10px 14px", color #FAFAFA, fontFamily Outfit, fontWeight 600, fontSize 0.9rem
     - Text input: type="tel", placeholder "Enter Your Mobile No", flex 1, background transparent, border none, outline none, color #FAFAFA, fontFamily Outfit, fontSize 0.9rem, fontWeight 400, placeholder color #52525B. Use a useState for the phone value.
   - Divider: 1px solid #1C1C1F, margin "14px 0"
   - Helper text: "We'll send a one-time code to verify your number." — Outfit, fontWeight 300, fontSize 0.78rem, lineHeight 1.55, color #52525B
   - Bold statement: "Your number stays linked to reminders, backup, and financial insights." — Outfit, fontWeight 600, fontSize 0.85rem, lineHeight 1.55, color #FAFAFA, marginTop 10
8. Flex spacer (flex: 1) to push button to bottom
9. "Send OTP" button — full width, padding 16px, background #C8FF00, color #050505, borderRadius 14, Outfit fontWeight 600, fontSize 1rem. Same press animation (scale 0.98) as OnboardingScreen CTA. Button is disabled (opacity 0.4) when phone input is empty; enabled (opacity 1) when phone has content. On click, navigate to /app (placeholder action).
10. Below button: "Don't have an account? Register" — center-aligned, Outfit 0.85rem, color #52525B for "Don't have an account?", then a Link to="/register" with color #C8FF00 for "Register"

Layout wrapper: same as OnboardingScreen — flex flex-col min-h-[100dvh], background #050505, paddingTop env(safe-area-inset-top), paddingBottom max(env(safe-area-inset-bottom), 16px). Content area padding 0 24px.

Stagger fadeUp animations: brand (0ms), eyebrow (80ms), heading (160ms), body (240ms), card (320ms), button (400ms), register link (440ms).

Import useState from react, useNavigate and Link from react-router-dom.
  </action>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Frontend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>LoginScreen renders full HIFI OTP sign-in layout with StatusBar, CostraBrand, eyebrow, heading, body, mobile input card with +94 prefix, Send OTP button (disabled when empty), register link, and staggered fadeUp animations. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update OnboardingScreen navigation to /login</name>
  <files>src/flows/onboarding/screens/OnboardingScreen.tsx</files>
  <action>
In OnboardingScreen.tsx, change both navigation targets from '/register' to '/login':

1. In the `next` function (line ~367): change `void navigate('/register')` to `void navigate('/login')`
2. In the `skip` function (line ~372): change `void navigate('/register')` to `void navigate('/login')`

No other changes to this file.
  </action>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Frontend" && grep -n "navigate.*login\|navigate.*register" src/flows/onboarding/screens/OnboardingScreen.tsx</automated>
  </verify>
  <done>Both skip() and next() on slide 4 navigate to /login. No references to /register remain in OnboardingScreen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>HIFI Login screen with OTP mobile number input, staggered animations, and onboarding-to-login navigation flow</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173
    2. Watch splash screen, then onboarding carousel
    3. On slide 1, tap "Skip for now" — should navigate to /login (not /register)
    4. Go back to onboarding, advance to slide 4, tap "Get Started" — should also go to /login
    5. On login screen verify:
       - StatusBar with 9:41 and icons at top
       - Costra logo + wordmark
       - "OTP SIGN-IN" lime eyebrow label
       - "Login with your mobile number." heading
       - Body paragraph in muted gray
       - Dark card with "+94" chip, phone input field, helper text, bold statement
       - "Send OTP" button is disabled/dimmed when input empty
       - Type a number — button becomes fully opaque
       - Staggered fade-up animations on page load
       - "Don't have an account? Register" link at bottom
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<success_criteria>
- LoginScreen shows full HIFI OTP sign-in matching the design (dark bg, lime accents, card with mobile input, animations)
- Phone input accepts text and controls Send OTP button enabled state
- OnboardingScreen skip and Get Started both navigate to /login
- TypeScript compiles with no errors
- Visual fidelity matches Splash and Onboarding screens in style consistency
</success_criteria>
