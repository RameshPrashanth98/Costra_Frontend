---
id: 260406-k4g
type: quick
description: Build Home screen flow — 5 HIFI screens with full navigation wiring
tasks: 5
files_modified:
  - src/layouts/AppLayout.tsx
  - src/router.tsx
  - src/flows/home/screens/HomeScreen.tsx
  - src/flows/notifications/screens/NotificationsScreen.tsx
  - src/flows/transactions/screens/TransactionDetailsScreen.tsx
  - src/flows/transactions/screens/AllTransactionsScreen.tsx
  - src/flows/voice-entry/screens/VoiceEntryScreen.tsx
autonomous: true
---

<objective>
Build the complete Home screen flow: rewrite AppLayout with Costra dark theme and BottomNav, build HomeScreen (HIFI dashboard), NotificationsScreen, TransactionDetailsScreen, AllTransactionsScreen, and VoiceEntryScreen. Wire all navigation: Login -> Home, bell icon -> Notifications, transaction tap -> Transaction Details, See All -> All Transactions, mic icon -> Voice Entry. All screens 100% HIFI fidelity with Costra design tokens, animations, iPhone 16 Pro (393px) sizing.

Purpose: Deliver the core app experience — the first screens users see after login.
Output: 6 files (1 layout rewrite + 5 screens) + router updates with new routes.
</objective>

<execution_context>
@.claude/skills/costra-m3-builder/SKILL.md
@.claude/skills/costra-m3-builder/references/costra-tokens.md
</execution_context>

<context>
@src/layouts/AuthLayout.tsx (pattern reference — dark theme, maxWidth 393, inline styles)
@src/flows/login/screens/LoginScreen.tsx (pattern reference — StatusBar, fadeUp, keyframes, Lucide icons)
@src/router.tsx (current routes — add new routes here)
@src/layouts/AppLayout.tsx (Phase 1 placeholder — complete rewrite)
@src/flows/home/screens/HomeScreen.tsx (Phase 1 placeholder — complete rewrite)

<interfaces>
<!-- Existing patterns from auth screens that MUST be reused -->

StatusBar pattern (from LoginScreen.tsx):
- Inline function component with 9:41 time, signal/wifi/battery SVGs
- Style: padding "12px 24px 8px", JetBrains Mono 0.8rem weight 600
- Each screen defines its own StatusBar (no shared component yet)

Animation pattern (from LoginScreen.tsx):
- KEYFRAMES const string with @keyframes declaration
- fadeUp(delay) helper returning CSSProperties with opacity:0, animation string
- Injected via <style>{KEYFRAMES}</style> at top of component return

Layout pattern (from AuthLayout.tsx):
- mx-auto flex min-h-[100dvh] flex-col
- style={{ maxWidth: 393, background: '#050505' }}

Navigation pattern:
- useNavigate() from react-router-dom for programmatic nav
- React.lazy + Suspense wrapping in router.tsx

Icon pattern:
- Import from lucide-react, strokeWidth={1.5}
- Verified safe: Home, List, BarChart3, Wallet, UserCircle, Bell, Mic, ArrowLeft, X,
  Search, Pen, Trash2, Plus, AlertTriangle, Lightbulb, ChevronRight, Utensils, Bus,
  ShoppingBag, Zap, Coffee, CreditCard, Calendar, Tag, FileText, Check, Info
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AppLayout with Costra dark theme + BottomNav, add new routes</name>
  <files>src/layouts/AppLayout.tsx, src/router.tsx</files>
  <action>
**AppLayout.tsx — complete rewrite:**

Replace the Phase 1 placeholder entirely. Structure:

1. Outer wrapper: `mx-auto flex min-h-[100dvh] flex-col` with `style={{ maxWidth: 393, background: '#050505' }}` (mirrors AuthLayout pattern)
2. Main content area: `flex-1 overflow-y-auto` with `<Outlet />`, padding-bottom enough to clear BottomNav (~80px + safe area)
3. Fixed BottomNav at bottom:
   - Container: fixed bottom, maxWidth 393, mx-auto
   - Style: `background: '#0A0A0B'`, `borderTop: '1px solid #1C1C1F'`, padding `8px 0 calc(env(safe-area-inset-bottom, 0px) + 4px)`
   - 5 tabs: HOME, TRACK, INSIGHTS, WALLET, PROFILE
   - Icons: `Home`, `List`, `BarChart3`, `Wallet`, `UserCircle` from lucide-react, size 22, strokeWidth 1.5
   - Labels: JetBrains Mono, 0.5rem, uppercase, letterSpacing 0.08em, fontWeight 500
   - Active state: icon+label color `#C8FF00`, background pill `rgba(200,255,0,0.12)` behind icon (borderRadius 9999, padding 6px 12px)
   - Inactive state: icon+label color `#52525B`
   - Use `useLocation()` from react-router-dom to determine active tab
   - Use `useNavigate()` for tab clicks — paths: `/app`, `/app/track`, `/app/insights`, `/app/wallet`, `/app/profile`
   - BottomNav accepts an optional `hide` prop (boolean) — when true, do not render the nav (for VoiceEntry modal screen)

4. AppLayout reads a `hideNav` state from a simple context or from the route. Simplest approach: check `location.pathname` — if it includes `voice-entry`, hide BottomNav.

**router.tsx — add 4 new routes:**

Add lazy imports at top:
```typescript
const NotificationsScreen = lazy(() => import('@/flows/notifications/screens/NotificationsScreen'));
const AllTransactionsScreen = lazy(() => import('@/flows/transactions/screens/AllTransactionsScreen'));
const TransactionDetailsScreen = lazy(() => import('@/flows/transactions/screens/TransactionDetailsScreen'));
const VoiceEntryScreen = lazy(() => import('@/flows/voice-entry/screens/VoiceEntryScreen'));
```

Add inside the `/app` children array:
```typescript
{ path: 'notifications', element: withSuspense(<NotificationsScreen />) },
{ path: 'transactions', element: withSuspense(<AllTransactionsScreen />) },
{ path: 'transaction/:id', element: withSuspense(<TransactionDetailsScreen />) },
{ path: 'voice-entry', element: withSuspense(<VoiceEntryScreen />) },
```

Do NOT change any existing routes or auth flow.
  </action>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Frontend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>AppLayout renders dark theme (#050505 bg) with functional BottomNav (5 tabs, lime active state, proper icons/labels). Router has all 4 new routes. BottomNav hides on voice-entry path. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build HIFI HomeScreen dashboard</name>
  <files>src/flows/home/screens/HomeScreen.tsx</files>
  <action>
Complete rewrite of HomeScreen.tsx. This is the largest screen — a scrollable dashboard. Follow LoginScreen patterns: local StatusBar, KEYFRAMES, fadeUp helper, inline styles for colors, Tailwind for layout.

**Structure (top to bottom, all scrollable above BottomNav):**

1. **Inject styles:** `<style>{KEYFRAMES}</style>` with fadeUp keyframes (same pattern as LoginScreen)

2. **StatusBar** — same as LoginScreen pattern (9:41, signal/wifi/battery SVGs)

3. **Header row** (padding 0 20px):
   - Left: Greeting text "Good Morning, Chathuranga" in Outfit 600 1rem #FAFAFA, below: "Wednesday, 25 March 2026" in JetBrains Mono 0.6rem #52525B uppercase tracking 0.1em
   - Right: Two icon buttons — `Bell` icon (size 22, #A1A1AA) with small red dot indicator (6px circle, #EF4444, absolute positioned top-right), `User` icon (size 22, #A1A1AA)
   - Bell onClick: `navigate('/app/notifications')`

4. **Hero "Money Left" card** (margin 0 20px, mt 20px):
   - Card bg #0A0A0B, border 1px solid #1C1C1F, borderRadius 16, padding 24px 20px
   - "MONEY LEFT TODAY" — JetBrains Mono 0.6rem #52525B uppercase tracking 0.15em
   - "₹1,245" — Outfit 900 2.6rem #FAFAFA lineHeight 1.1
   - "Safe" badge — inline pill: bg #22C55E20, color #22C55E, borderRadius 9999, padding 4px 12px, JetBrains Mono 0.6rem uppercase fontWeight 600
   - Row below: "Income" label + "₹2,500" (green #22C55E) | divider | "Spent" label + "₹1,255" (red #EF4444)
   - Labels in JetBrains Mono 0.55rem #52525B uppercase

5. **"Today's Spending" section** (padding 0 20px, mt 24px):
   - Section header: "Today's Spending" Outfit 700 1rem #FAFAFA + "₹1,255" right-aligned Outfit 600 0.9rem #FAFAFA
   - 3 horizontal category cards (flex row, gap 10, overflow-x auto):
     - Food ₹580 (icon: `Utensils`, color #F59E0B)
     - Transport ₹375 (icon: `Bus`, color #3B82F6)
     - Shopping ₹300 (icon: `ShoppingBag`, color #A855F7 — use purple for shopping)
   - Each card: bg #0A0A0B, border 1px solid #1C1C1F, borderRadius 14, padding 16px, minWidth 120
   - Icon in 36px circle (bg matching icon color at 15% opacity), category name JetBrains Mono 0.55rem #A1A1AA uppercase, amount Outfit 600 0.95rem #FAFAFA

6. **Alert banners** (padding 0 20px, mt 16px, flex col gap 10):
   - Warning: bg #111113, border 1px solid #1C1C1F, borderRadius 12, padding 14px 16px. Row: `AlertTriangle` icon size 18 color #F59E0B, text "You may run out by evening at this rate" Outfit 400 0.8rem #A1A1AA
   - Insight: bg rgba(200,255,0,0.06), border 1px solid rgba(200,255,0,0.12), borderRadius 12, padding 14px 16px. Row: `Lightbulb` icon size 18 color #C8FF00, text "Unusual spending on Food today (+40%)" Outfit 400 0.8rem #A1A1AA

7. **"Where Your Money Went Today"** (padding 0 20px, mt 24px):
   - Section label: JetBrains Mono 0.6rem #C8FF00 uppercase tracking 0.2em
   - Bullet list (3 items): left dot (6px, #C8FF00), text Outfit 400 0.85rem #A1A1AA, amount right Outfit 500 0.85rem #FAFAFA
   - Items: "Tea & Snacks x4" ₹180, "Auto Rickshaw x2" ₹175, "Online Shopping" ₹300

8. **"Recent Transactions"** (padding 0 20px, mt 24px):
   - Header row: "Recent Transactions" Outfit 700 1rem #FAFAFA + "See All" link (Outfit 500 0.8rem #C8FF00, onClick navigate('/app/transactions'))
   - 3 transaction rows, each clickable (onClick navigate('/app/transaction/1') etc.):
     - Row: icon container (40x40, bg #111113, border 1px solid #1C1C1F, borderRadius 12) + name/category/time + amount
     - Morning Coffee | Food | 8:30 AM | -₹45 (icon: `Coffee`)
     - Bus Fare | Transport | 9:15 AM | -₹25 (icon: `Bus`)
     - Amazon Order | Shopping | 11:45 AM | -₹300 (icon: `ShoppingBag`)
     - Name: Outfit 500 0.9rem #FAFAFA, Category+time: JetBrains Mono 0.55rem #52525B, Amount: Outfit 600 0.9rem #FAFAFA

9. **"Wallet" section** (padding 0 20px, mt 24px):
   - Section label: "WALLET" JetBrains Mono 0.6rem #C8FF00 uppercase tracking 0.2em
   - 2 horizontal cards (flex row gap 10):
     - Cash ₹2,450 — icon `Wallet` color #C8FF00
     - Bank ₹18,750 — icon `Landmark` (WAIT — not in safe list, use `CreditCard` instead)
   - Each: bg #0A0A0B, border 1px solid #1C1C1F, borderRadius 14, padding 16px, flex 1

10. **Action bar** (padding 0 20px, mt 24px, mb 24px):
    - Row with 3 elements:
      - "+ Add Expense" button: flex 1, bg #C8FF00, color #050505, borderRadius 14, padding 14px, Outfit 600 0.85rem
      - "+ Add Income" button: flex 1, bg transparent, border 1px solid #27272A, color #FAFAFA, borderRadius 14, padding 14px, Outfit 600 0.85rem
      - Mic icon button: width 48, height 48, bg rgba(200,255,0,0.12), borderRadius 14, `Mic` icon color #C8FF00 size 22
      - Mic onClick: `navigate('/app/voice-entry')`

Apply fadeUp animations staggered across sections (100ms increments starting at 100ms). Wrap all in a scrollable div with paddingBottom 20px.
  </action>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Frontend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>HomeScreen renders full HIFI dashboard with all 10 sections. Bell navigates to /app/notifications. See All navigates to /app/transactions. Transaction rows navigate to /app/transaction/:id. Mic navigates to /app/voice-entry. All colors match Costra tokens exactly. Staggered fadeUp animations on load.</done>
</task>

<task type="auto">
  <name>Task 3: Build HIFI NotificationsScreen + TransactionDetailsScreen</name>
  <files>src/flows/notifications/screens/NotificationsScreen.tsx, src/flows/transactions/screens/TransactionDetailsScreen.tsx</files>
  <action>
Create the two flow directories and screen files. Each follows established patterns: local StatusBar, KEYFRAMES, fadeUp, inline styles for colors, Tailwind for layout, Lucide icons with strokeWidth 1.5.

**NotificationsScreen.tsx:**

1. Styles + StatusBar (same pattern)
2. **Top bar:** `ArrowLeft` icon (onClick navigate(-1)), "NOTIFICATIONS" center text (JetBrains Mono 0.7rem #FAFAFA uppercase tracking 0.15em fontWeight 600), "MARK ALL AS READ" right link (JetBrains Mono 0.55rem #C8FF00 uppercase)
3. **Filter chips row** (flex row gap 8, overflow-x auto, padding 0 20px, mt 16px):
   - ALL (active — lime bg), ALERTS, REMINDERS, INCOME (inactive — border #27272A)
   - Use useState for activeFilter, default 'ALL'
   - Chip styles from SKILL.md filter chip patterns
4. **"TODAY" section** (mt 20px, padding 0 20px):
   - Label: JetBrains Mono 0.6rem #52525B uppercase tracking 0.15em, mb 12px
   - 4 notification cards (flex col gap 10):
     - Each card: bg #0A0A0B, border 1px solid #1C1C1F, borderRadius 14, padding 16px
     - Left colored border: borderLeft 3px solid {color} — red (#EF4444) for budget warnings, #1C1C1F for neutral, blue (#3B82F6) for income
     - Title: Outfit 500 0.85rem #FAFAFA
     - Body/time: JetBrains Mono 0.55rem #52525B
     - "View details" link: Outfit 500 0.75rem #C8FF00 with ChevronRight icon size 14
   - Cards:
     a. Red border: "You might run out of budget today" — 15 min ago — "View details ->"
     b. Neutral: "You spent more than usual on food this week" — 2h ago
     c. Neutral: "Don't forget to add your expenses for today" — 5h ago
     d. Blue border: "Salary of ₹45,000 received to HDFC account" — 8h ago
5. **"YESTERDAY" section** (mt 24px):
   - Same label pattern
   - 2 more notification cards:
     a. Neutral: "Weekly spending report is ready" — Yesterday
     b. Neutral: "New feature: Voice entry is now available!" — Yesterday
6. BottomNav visible (standard AppLayout handles it)
7. FadeUp animations staggered

**TransactionDetailsScreen.tsx:**

1. Styles + StatusBar
2. **Top bar:** `ArrowLeft` (navigate(-1)), "Transaction Details" center (Outfit 600 0.95rem #FAFAFA), `Pen` icon right (color #A1A1AA, size 20)
3. **Expense badge** (centered, mt 20px): pill with bg rgba(200,255,0,0.12), borderRadius 9999, padding 6px 16px. Contains `ArrowUpRight` icon size 16 color #C8FF00 + "Expense" JetBrains Mono 0.6rem #C8FF00 uppercase
4. **Amount** (centered, mt 16px): "₹ 450.00" Outfit 900 2.6rem #FAFAFA
5. **Category** (centered, mt 8px): icon container (bg #111113 border #1C1C1F borderRadius 12, 36x36) with `Utensils` icon + "Food & Dining" Outfit 500 0.85rem #A1A1AA
6. **Details card** (margin 0 20px, mt 24px): bg #0A0A0B, border 1px solid #1C1C1F, borderRadius 16, padding 20px
   - 3 rows with dividers (borderBottom 1px solid #1C1C1F, padding 14px 0):
     - "Date & Time" (label) — "25 Mar 2026, 12:30 PM" (value). Label: JetBrains Mono 0.55rem #52525B uppercase. Value: Outfit 400 0.85rem #FAFAFA
     - "Payment Method" — "Cash"
     - "Category" — "Food & Dining"
   - Use `Calendar`, `CreditCard`, `Tag` icons for each row (size 16, color #52525B)
7. **Notes section** (margin 0 20px, mt 20px):
   - Label: JetBrains Mono 0.6rem #52525B uppercase tracking 0.15em
   - Text: "Lunch at office canteen with colleagues" Outfit 400 0.85rem #A1A1AA, mt 8px
8. **Insight banner** (margin 0 20px, mt 20px): bg rgba(200,255,0,0.06), border rgba(200,255,0,0.12), borderRadius 14, padding 14px 16px. `Lightbulb` icon #C8FF00 + "This is ₹120 higher than your usual lunch spend" Outfit 400 0.8rem #A1A1AA
9. **Buttons** (margin 0 20px, mt 24px, flex col gap 12):
   - "Edit Transaction" — primary CTA (lime bg, dark text, borderRadius 14, full width, padding 16px)
   - "Delete Transaction" — danger outlined (transparent bg, border rgba(239,68,68,0.3), color #EF4444, borderRadius 14)
10. FadeUp animations, bottom padding for BottomNav
  </action>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Frontend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>NotificationsScreen renders with filter chips (active state toggle), colored-border notification cards in TODAY/YESTERDAY groups, back navigation. TransactionDetailsScreen renders expense detail with amount, category, details card, notes, insight banner, edit/delete buttons. Both match HIFI exactly.</done>
</task>

<task type="auto">
  <name>Task 4: Build HIFI AllTransactionsScreen + VoiceEntryScreen</name>
  <files>src/flows/transactions/screens/AllTransactionsScreen.tsx, src/flows/voice-entry/screens/VoiceEntryScreen.tsx</files>
  <action>
Same patterns: local StatusBar, KEYFRAMES, fadeUp, inline styles, Lucide icons.

**AllTransactionsScreen.tsx:**

1. Styles + StatusBar
2. **Top bar:** `ArrowLeft` (navigate(-1)), "All Transactions" center (Outfit 600 0.95rem #FAFAFA)
3. **Search bar** (margin 0 20px, mt 16px): bg #0A0A0B, border 1px solid #1C1C1F, borderRadius 14, padding 12px 16px. Row: `Search` icon size 18 color #52525B + input placeholder "Search transactions..." (Outfit 400 0.85rem, placeholder color #52525B). Input: bg transparent, border none, color #FAFAFA, outline none, flex 1
4. **Filter chips row** (flex row gap 8, padding 0 20px, mt 14px, overflow-x auto):
   - ALL (active lime), Food, Transport, Bills, Shopping (inactive)
   - useState for activeFilter, default 'ALL'
5. **Date group: "Today, 26 Mar"** (padding 0 20px, mt 20px):
   - Header row: date label (JetBrains Mono 0.6rem #52525B uppercase tracking 0.1em) + total "-₹1,370" (Outfit 500 0.8rem #EF4444)
   - Transaction rows (flex col gap 2, each clickable -> navigate('/app/transaction/{id}')):
     - Each row: padding 14px 0, flex row, items-center
     - Left: icon container 40x40 bg #111113 border #1C1C1F borderRadius 12
     - Middle: name Outfit 500 0.85rem #FAFAFA, below: category + time JetBrains Mono 0.55rem #52525B
     - Right: amount Outfit 600 0.85rem #FAFAFA (negative amounts, no color — just white)
     - Divider: borderBottom 1px solid #1C1C1F between rows
   - Transactions:
     a. Morning Coffee | Food | 8:30 AM | -₹45 (Coffee icon)
     b. Bus Fare | Transport | 9:15 AM | -₹25 (Bus icon)
     c. Amazon Order | Shopping | 11:45 AM | -₹300 (ShoppingBag icon)
     d. Electricity Bill | Bills | 2:00 PM | -₹850 (Zap icon)
     e. Grocery Store | Food | 4:30 PM | -₹150 (Utensils icon)
6. **Date group: "Yesterday, 25 Mar"** (mt 20px):
   - Header: date + total "-₹980"
   - 3 transactions:
     a. Lunch | Food | 12:30 PM | -₹450 (Utensils icon)
     b. Auto Rickshaw | Transport | 3:15 PM | -₹175 (Bus icon)
     c. Movie Tickets | Entertainment | 7:00 PM | -₹355 (use `Music` icon as safe fallback)
7. FadeUp animations, bottom padding

**VoiceEntryScreen.tsx:**

This is a modal-like full-screen experience. BottomNav should NOT show (AppLayout hides nav when pathname includes 'voice-entry').

1. **Inject styles:** KEYFRAMES for fadeUp + additional keyframes:
   - `@keyframes voicePulse { 0%, 100% { transform: scale(1); opacity: 0.3; } 50% { transform: scale(1.3); opacity: 0.1; } }` for the glow circles
   - `@keyframes waveform { 0%, 100% { height: 8px; } 50% { height: 24px; } }` for waveform dots

2. **Top bar** (padding 12px 20px): `ArrowLeft` (navigate(-1)) left, `X` icon right (also navigate(-1))

3. **Label + Title** (padding 0 20px, mt 12px):
   - "VOICE ENTRY" — JetBrains Mono 0.6rem #C8FF00 uppercase tracking 0.2em
   - "Tell Costra what you spent." — Outfit 800 1.8rem #FAFAFA lineHeight 1.15, mt 8px

4. **Listening card** (margin 0 20px, mt 32px): bg #0A0A0B, border 1px solid #1C1C1F, borderRadius 16, padding 32px, textAlign center
   - Mic icon area: centered, relative. `Mic` icon size 32 color #C8FF00 in a 64px circle (bg rgba(200,255,0,0.12), borderRadius 9999)
   - 2 concentric pulse rings behind mic: absolute, 80px and 100px circles, border 1px solid rgba(200,255,0,0.15), borderRadius 9999, animation voicePulse 2s ease infinite (second ring delayed 0.5s)
   - "Listening now" label below: JetBrains Mono 0.6rem #C8FF00 uppercase tracking 0.15em, mt 16px
   - Prompt text: "Say something like 'Spent 650 on groceries and 120 for bus fare.'" Outfit 300 0.8rem #52525B, mt 8px, maxWidth 260, mx-auto

5. **Waveform dots** (centered row, mt 20px): 7 dots, each 4px wide, borderRadius 9999, bg #C8FF00, varying heights animated with waveform keyframe at different delays (0s, 0.1s, 0.2s, etc.)

6. **"LIVE TRANSCRIPT" section** (padding 0 20px, mt 28px):
   - Label: JetBrains Mono 0.6rem #52525B uppercase tracking 0.15em
   - Transcript text: "Spent seven hundred and seventy rupees at Keells, groceries and bus fare" — Outfit 400 0.9rem #FAFAFA, mt 10px, bg #0A0A0B, border 1px solid #1C1C1F, borderRadius 14, padding 16px

7. **Parsed result card** (margin 0 20px, mt 20px): bg #0A0A0B, border 1px solid #1C1C1F, borderRadius 16, padding 20px
   - 4 rows, each with label + value:
     - AMOUNT: "₹770" (label JetBrains Mono 0.55rem #52525B uppercase, value Outfit 600 0.95rem #FAFAFA)
     - TYPE: "Expense" with ArrowUpRight icon
     - MERCHANT: "Keells"
     - CATEGORY: "Groceries + Travel" — each as a small pill (bg #111113, border #1C1C1F, borderRadius 9999, padding 4px 10px, JetBrains Mono 0.55rem #A1A1AA)
   - Rows separated by borderBottom 1px solid #1C1C1F, padding 12px 0

8. **CTA buttons** (padding 0 20px, mt 24px, mb 32px):
   - "Create Transaction Draft" — primary lime CTA (full width, bg #C8FF00, color #050505, borderRadius 14, padding 16px, Outfit 600 1rem)
   - Row below (flex row gap 12, mt 12px):
     - "Try Again" — flex 1, outlined (border #27272A, color #FAFAFA, borderRadius 14, padding 14px, Outfit 500 0.85rem)
     - "Type Instead" — flex 1, outlined same style

9. FadeUp animations staggered across sections
  </action>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Frontend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>AllTransactionsScreen renders with search bar, filter chips, date-grouped transaction list with clickable rows. VoiceEntryScreen renders full modal experience with animated pulse rings, waveform dots, live transcript, parsed result card, and CTA buttons. BottomNav hidden on VoiceEntry. Both match HIFI exactly.</done>
</task>

<task type="auto">
  <name>Task 5: Verify full navigation flow and production build</name>
  <files>src/flows/login/screens/LoginScreen.tsx</files>
  <action>
1. **Update LoginScreen sign-in navigation:** In LoginScreen.tsx, find the sign-in button's onClick handler. It currently navigates somewhere after the OTP flow. Update the final navigation destination to `/app` (the Home screen). If there is already a navigate('/app') call, leave it. If it navigates elsewhere (like '/app/home' or stays on auth), change it to `navigate('/app')`.

2. **Verify the build compiles:** Run `npm run build` and confirm zero errors. Fix any TypeScript errors, missing imports, or broken references.

3. **Verify all routes resolve:** Confirm these paths exist in the router:
   - `/app` -> HomeScreen
   - `/app/notifications` -> NotificationsScreen
   - `/app/transactions` -> AllTransactionsScreen
   - `/app/transaction/:id` -> TransactionDetailsScreen
   - `/app/voice-entry` -> VoiceEntryScreen

4. **Run dev server and confirm no console errors** with `npm run dev` (just start it to verify it boots without error).
  </action>
  <verify>
    <automated>cd "D:/1.Product Development with AI/1.1 project/4.Cost of living tracker/Frontend" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Full build succeeds with zero errors. Login redirects to /app (Home). All 5 new screens are routable. Navigation flow works: Home -> Notifications (bell), Home -> All Transactions (See All), Home -> Transaction Detail (row tap), Home -> Voice Entry (mic icon). Back arrows navigate(-1) on all sub-screens.</done>
</task>

</tasks>

<verification>
After all 5 tasks:
1. `npm run build` produces zero errors
2. `npx tsc --noEmit` clean
3. Navigate from Login -> Home -> each sub-screen via intended triggers
4. BottomNav shows on Home, Notifications, Transactions, Transaction Details
5. BottomNav hides on Voice Entry
6. All screens use #050505 background, #C8FF00 accent, Outfit/JetBrains Mono fonts
7. All borderRadius values match SKILL.md (14-16px cards, 9999px pills, 12px icons)
</verification>

<success_criteria>
- 6 files created/rewritten (AppLayout + 5 screens)
- Router has 4 new routes
- Production build succeeds
- All navigation paths functional
- 100% HIFI fidelity to Costra design tokens
- Animations (fadeUp, voicePulse, waveform) working
- iPhone 16 Pro sizing (393px max-width) on all screens
</success_criteria>
