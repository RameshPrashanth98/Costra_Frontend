---
phase: quick-260406-hf9
task: 1
total_tasks: 1
status: in_progress
last_updated: "2026-04-06T07:07:42.117Z"
---

<current_state>
Quick task 260406-hf9: Fix logo SVG to match real Costra brand mark across all screens.

Actual logo PNG files have been copied to `public/logos/`. SplashScreen LogoMark has been partially updated (replaced SVG with img tag for costra-icon.png). Still need to fix remaining screens.

Previous quick tasks (login 260406-gi5, register 260406-gun) are committed but their STATE.md updates and summaries are still pending.
</current_state>

<completed_work>

- Copied actual logo files to `public/logos/costra-icon.png` and `public/logos/costra-logo-stacked.png`
- Updated SplashScreen.tsx: replaced LogoMark SVG circle+stroke with `<img src="/logos/costra-icon.png">` at 128x128
- Analyzed all 4 logo variants provided by user (App Icon, Logo Dark, Logo Stacked, Logo Transparent)
- Identified the issue: current SVGs use stroked outline paths, real logo is a solid filled block C
</completed_work>

<remaining_work>

- Fix SplashScreen WordmarkIcon (small 14x14 C mark next to "COSTRA" text) — replace stroked SVG with actual brand asset or fixed filled SVG
- Fix OnboardingScreen CostraBrand (32x32 lime square + "COSTRA" text) — replace SVG C with actual icon or filled path
- Fix LoginScreen CostraBrand (same as onboarding)
- Fix RegisterScreen CostraBrand (same as onboarding)
- Run TypeScript check to verify no errors
- Commit all logo fixes atomically
- Update STATE.md with quick task completion
- Also: finalize pending summaries/STATE updates for login (260406-gi5) and register (260406-gun) quick tasks
</remaining_work>

<decisions_made>

- Use actual PNG logo files (`costra-icon.png`) instead of SVG approximations for pixel-perfect accuracy
- For large splash logo (128x128): use costra-icon.png as img tag with borderRadius 16
- For small brand marks (32x32): plan to use costra-icon.png at 32x32 with borderRadius 4 — replaces both the lime div container AND the SVG C mark
- Logo files stored in `public/logos/` directory
</decisions_made>

<blockers>
None — straightforward remaining work.
</blockers>

<context>
The CostraBrand component pattern is duplicated across 3 files (onboarding, login, register) with identical code. Each has:
1. A 32x32 lime div with borderRadius 4
2. An 18x18 SVG inside with a stroked C path
3. "COSTRA" text span next to it

The fix for all 3 is identical: replace the lime div + SVG with an `<img src="/logos/costra-icon.png" width={32} height={32} style={{ borderRadius: 4 }}>`.

SplashScreen has two logo elements: LogoMark (128x128, already fixed) and WordmarkIcon (14x14 small C, still needs fix).
</context>

<next_action>
Start with: Fix WordmarkIcon in SplashScreen.tsx (replace 14x14 stroked SVG), then update CostraBrand in OnboardingScreen.tsx, LoginScreen.tsx, RegisterScreen.tsx with img tag approach. Run tsc --noEmit, commit all changes.
</next_action>
